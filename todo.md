# @liquicode/jsonproc


# Project TODO

- A Playground page, the way `jsongin` has one.
  It would have to load two bundles rather than one, since jsongin is external to this one.
- Recipes. The worked patterns live inside `Step-Operators.md` today — *Fanning Out* in
  particular — and would read better as pages of their own once there are more of them.

***And the standing non-goals***, which are not a work list and are not to be smuggled in:
  `async` inside `src/`, closures and user-defined procedures, a continuation object beyond the
  run value itself, a caller scope carried into `Query()`, and a parallel step operator.
